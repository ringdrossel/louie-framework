Admin-facing features for managing the running application — settings, users, audit log, and similar controls.

Recipes in this section assume a logged-in admin audience and an existing auth / authz mechanism in the project. They focus on the admin surface itself (data model, UI, CRUD behavior) and let the project's existing auth handle access control.
